import { json } from './_auth-core.js';
import { singleQueryValue } from './_request-query.js';
import { ownerBroker } from './_owner-broker-client.js';
const DAB=/^DAB-[0-9]{6,}$/i;const n=v=>Number.isFinite(Number(v))?Number(v):0;
function diagnoseBusiness({business,billing,whatsapp,products,inventory,orders,channels,audit,supportCases}){const findings=[],stock=new Map((inventory||[]).map(x=>[x.product_id,x]));const add=(severity,code,title,why,next)=>findings.push({severity,code,title,why,next});const bs=billing?.[0];if(!bs)add('warning','BILLING_MISSING','لا يوجد اشتراك مسجل','لا يوجد سجل فوترة لهذا النشاط.','تحقق هل النشاط جديد أو لم تبدأ له خطة بعد.');else if(!['active','trialing'].includes(String(bs.status||'').toLowerCase()))add('critical','BILLING_INACTIVE','الاشتراك غير نشط',`حالة الاشتراك الحالية: ${bs.status||'unknown'}.`,'راجع الفوترة قبل تشخيص وظائف مدفوعة أخرى.');const w=whatsapp?.[0];if(w&&!['connected','operational','verified','live'].includes(String(w.status||'').toLowerCase()))add('warning','WHATSAPP_DEGRADED','WhatsApp غير جاهز',`الحالة الحالية: ${w.status||'unknown'}${w.last_error?` · ${String(w.last_error).slice(0,120)}`:''}.`,'راجع آخر تحقق واتصال Meta قبل طلب إعادة الربط.');const low=[];for(const p of products||[]){if(p.active===false)continue;const s=stock.get(p.id)||{},available=Math.max(0,n(s.quantity)-n(s.reserved));if(available<=5)low.push({name:p.name||p.sku||'منتج',available})}if(low.some(x=>x.available===0))add('critical','OUT_OF_STOCK','يوجد مخزون نافد',low.filter(x=>x.available===0).slice(0,4).map(x=>x.name).join('، '),'راجع الجرد أو استلام المخزون قبل اعتبار فشل الطلب خللًا تقنيًا.');else if(low.length)add('warning','LOW_STOCK','يوجد مخزون منخفض',`${low.length} منتج عند حد 5 وحدات أو أقل.`,'راجع المخزون إذا كانت الشكوى عن عدم توفر منتج أو طلب.');const pending=(orders||[]).filter(o=>o.simulated===false&&['draft','reserved'].includes(String(o.status||'').toLowerCase()));if(pending.length)add('warning','PENDING_ORDERS','طلبات تحتاج متابعة',`${pending.length} طلب بحالة draft/reserved.`,'افتح الطلبات وحدد هل المشكلة في المخزون أو إكمال الطلب.');const openSupport=(supportCases||[]).filter(c=>['open','waiting'].includes(String(c.status||'').toLowerCase()));if(openSupport.length)add('info','OPEN_SUPPORT','للعميل قضية دعم مفتوحة',`${openSupport.length} قضية دعم مفتوحة/انتظار.`,'اقرأ آخر ملاحظة قبل سؤال العميل عن معلومات سبق أن أعطاها.');if((audit||[]).length)add('info','RECENT_OWNER_CHANGES','توجد تغييرات إدارية حديثة',`آخر إجراء: ${audit[0].action||'unknown'} · ${audit[0].created_at||''}`,'قارن وقت بداية المشكلة مع آخر تغيير قبل تنفيذ تعديل جديد.');const bad=(channels||[]).filter(c=>!['connected','operational','verified','live'].includes(String(c.status||'').toLowerCase()));if(bad.length)add('info','CHANNELS_REVIEW','قنوات تحتاج مراجعة',bad.slice(0,3).map(c=>`${c.channel_type||'channel'}: ${c.status||'unknown'}`).join(' · '),'إذا كانت الشكوى مرتبطة بقناة، ابدأ بالقناة نفسها قبل بقية النظام.');if(!findings.some(x=>x.severity==='critical'||x.severity==='warning'))add('ok','NO_OBVIOUS_INCIDENT','لا يوجد سبب تشغيلي واضح','الاشتراك والقنوات والمخزون والطلبات لا تظهر عطلًا مباشرًا وفق البيانات الحالية.','اطلب وصف الخطأ ووقته ثم قارنه بالـTimeline؛ لا تغيّر بيانات دون دليل.');const weight={critical:3,warning:2,info:1,ok:0};findings.sort((a,b)=>(weight[b.severity]||0)-(weight[a.severity]||0));return{business:{id:business.id,name:business.name||'—',business_type:business.business_type||null},findings,summary:{critical:findings.filter(x=>x.severity==='critical').length,warning:findings.filter(x=>x.severity==='warning').length,open_orders:pending.length,low_stock:low.length}}}
export default async function handler(req,res){res.setHeader('cache-control','no-store, max-age=0');if(req.method!=='GET')return json(res,405,{ok:false,error:'METHOD_NOT_ALLOWED'},{allow:'GET'});const customerNo=String(singleQueryValue(req,'customer_no')||'').trim().toUpperCase();if(!DAB.test(customerNo))return json(res,400,{ok:false,error:'INVALID_CUSTOMER_NUMBER'});const{status,payload}=await ownerBroker(req,'diagnostic_raw',{customer_no:customerNo});if(status===401)return json(res,401,{ok:false,error:'OWNER_SESSION_REQUIRED'});if(status!==200||!payload?.ok)return json(res,status>=500?503:status,{ok:false,error:payload?.error||'OWNER_DIAGNOSTIC_FAILED'});const support=payload.support||{},cases=Array.isArray(support.cases)?support.cases:[],businesses=[],timeline=[];for(const row of Array.isArray(payload.businesses)?payload.businesses:[]){const id=row.business?.id;if(!id)continue;const bc=cases.filter(c=>!c.business_id||c.business_id===id);businesses.push(diagnoseBusiness({...row,supportCases:bc}));for(const a of (row.audit||[]).slice(0,10))timeline.push({at:a.created_at,type:'owner_action',business_id:id,title:a.action,detail:a.reason||a.outcome||''});for(const o of (row.orders||[]).slice(0,10))if(o.simulated===false)timeline.push({at:o.created_at,type:'order',business_id:id,title:`طلب ${o.status||'unknown'}`,detail:`${n(o.total_aed).toFixed(2)} AED`})}for(const c of cases.slice(0,20))timeline.push({at:c.updated_at||c.created_at,type:'support',business_id:c.business_id||null,title:c.subject||'قضية دعم',detail:`${c.status||'open'} · ${c.priority||'normal'}`});timeline.sort((a,b)=>new Date(b.at||0)-new Date(a.at||0));const all=businesses.flatMap(x=>x.findings),critical=all.filter(x=>x.severity==='critical').length,warning=all.filter(x=>x.severity==='warning').length;return json(res,200,{ok:true,customer_no:customerNo,account:{user_id:payload.account?.user_id,memberships:(payload.memberships||[]).map(m=>({business_id:m.business_id,role:m.role,status:m.status}))},support:{metrics:support.metrics||{},cases},businesses,timeline:timeline.slice(0,40),incident:{state:critical?'critical':warning?'warning':'clear',critical,warning,recommended_start:all[0]?.next||'اقرأ Timeline ثم اطلب وصف الخطأ ووقته.'},checked_at:new Date().toISOString()})}
