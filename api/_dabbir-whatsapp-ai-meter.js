import { createHash } from 'node:crypto';
import { generateDABBIRAiReply as generateCoreReply } from './_ai-core.js';
import { SUPABASE_URL } from './_auth-core.js';
import { supabaseKeyHeaders } from './_supabase-key-auth.js';

const GATEWAY_ENDPOINT='https://ai-gateway.vercel.sh/v1/chat/completions';
const PROVIDER_COOLDOWN_DEFAULT_MS=60_000;
const PROVIDER_COOLDOWN_MAX_MS=5*60_000;
const PROVIDER_429_STRIKE_WINDOW_MS=30_000;
const PROVIDER_NETWORK_STRIKE_WINDOW_MS=60_000;
const PROVIDER_NETWORK_COOLDOWN_MS=30_000;
const providerCooldowns=new Map();
const provider429Strikes=new Map();
const providerNetworkStrikes=new Map();
const transportIds=new WeakMap();let nextTransportId=1;
const clean=(value,max=400)=>String(value??'').trim().slice(0,max);
const finite=value=>Number.isFinite(Number(value))?Number(value):0;
const int=value=>Math.max(0,Math.trunc(finite(value)));
const hash=value=>createHash('sha256').update(String(value)).digest('hex');
const providerForEndpoint=endpoint=>endpoint===GATEWAY_ENDPOINT?'vercel-ai-gateway':endpoint.includes('groq.com')?'groq':endpoint.includes('generativelanguage.googleapis.com')?'google-gemini':endpoint.includes('cloudflare.com')?'cloudflare-workers-ai':'unknown';
const authorizationValue=headers=>String(headers?.authorization||headers?.Authorization||'');
function transportScope(fetchImpl){if(fetchImpl===fetch)return'native';if(!transportIds.has(fetchImpl))transportIds.set(fetchImpl,nextTransportId++);return`custom-${transportIds.get(fetchImpl)}`}
const cooldownKey=(provider,headers,fetchImpl)=>`${provider}:${hash(authorizationValue(headers)).slice(0,16)}:${transportScope(fetchImpl)}`;
function retryAfterMs(response,now=Date.now()){const value=clean(response?.headers?.get?.('retry-after'),80);if(!value)return null;const seconds=Number(value);if(Number.isFinite(seconds)&&seconds>=0)return Math.min(PROVIDER_COOLDOWN_MAX_MS,Math.max(1_000,Math.ceil(seconds*1000)));const at=Date.parse(value);if(!Number.isFinite(at))return null;return Math.min(PROVIDER_COOLDOWN_MAX_MS,Math.max(1_000,at-now))}
function cooldownRemainingMs(key,now=Date.now()){const until=Number(providerCooldowns.get(key)||0);if(until<=now){if(until)providerCooldowns.delete(key);return 0}return until-now}
function armProviderCooldown(key,duration,now=Date.now()){const until=now+duration;providerCooldowns.set(key,Math.max(Number(providerCooldowns.get(key)||0),until));provider429Strikes.delete(key);providerNetworkStrikes.delete(key);return duration}
function observeProvider429(key,response,now=Date.now()){const explicitRetry=retryAfterMs(response,now);if(explicitRetry!=null)return {retryAfterMs:explicitRetry,cooldownMs:armProviderCooldown(key,explicitRetry,now),strikeCount:1};const previous=provider429Strikes.get(key),consecutive=previous&&now-previous.at<=PROVIDER_429_STRIKE_WINDOW_MS?previous.count+1:1;provider429Strikes.set(key,{count:consecutive,at:now});if(consecutive<2)return {retryAfterMs:null,cooldownMs:null,strikeCount:consecutive};return {retryAfterMs:null,cooldownMs:armProviderCooldown(key,PROVIDER_COOLDOWN_DEFAULT_MS,now),strikeCount:consecutive}}
function observeProviderNetworkFailure(key,now=Date.now()){const previous=providerNetworkStrikes.get(key),consecutive=previous&&now-previous.at<=PROVIDER_NETWORK_STRIKE_WINDOW_MS?previous.count+1:1;providerNetworkStrikes.set(key,{count:consecutive,at:now});if(consecutive<2)return {cooldownMs:null,strikeCount:consecutive};return {cooldownMs:armProviderCooldown(key,PROVIDER_NETWORK_COOLDOWN_MS,now),strikeCount:consecutive}}
function clearProviderFailures(key){provider429Strikes.delete(key);providerNetworkStrikes.delete(key)}
function contextIdentity(raw){try{const value=typeof raw==='string'?JSON.parse(raw):raw,history=Array.isArray(value?.conversation_history)?value.conversation_history:[],last=history.at(-1)||{};return {businessId:clean(value?.business?.id,80),conversationId:clean(value?.conversation?.id,80),messageTimestamp:clean(value?.batch_message_created_at||last?.created_at,80)}}catch{return {businessId:'',conversationId:'',messageTimestamp:''}}}
function usageFromPayload(payload={}){const usage=payload?.usage||{};return {inputTokens:int(usage.prompt_tokens??usage.input_tokens),outputTokens:int(usage.completion_tokens??usage.output_tokens),reasoningTokens:int(usage?.completion_tokens_details?.reasoning_tokens??usage.reasoning_tokens)}}
export function actualGatewayCost(payload={},response){const candidates=[payload?.providerMetadata?.gateway?.cost,payload?.provider_metadata?.gateway?.cost,payload?.usage?.cost,payload?.cost,response?.headers?.get?.('x-vercel-ai-gateway-cost')];for(const value of candidates){if(typeof value!=='number'&&(typeof value!=='string'||!value.trim()))continue;const number=Number(value);if(Number.isFinite(number)&&number>=0)return number}return null}
async function recordUsage({businessId,operationKey,result,attempts,skippedAttempts,usage,actualCostUsd}){const key=clean(process.env.SUPABASE_SERVICE_ROLE_KEY,8192);if(!businessId||!key||key.startsWith('sb_publishable_'))return {ok:false,state:'METER_NOT_CONFIGURED'};const provider=clean(result?.provider,120)||'unknown',gateway=provider==='vercel-ai-gateway';const body={p_business_id:businessId,p_operation_key:operationKey,p_operation_type:'whatsapp.customer_ai_reply',p_channel:'whatsapp',p_provider:provider,p_model:clean(result?.model,160)||null,p_cost_mode:gateway?'PAID_FALLBACK':'FREE_FIRST_DIRECT',p_input_tokens:usage.inputTokens,p_output_tokens:usage.outputTokens,p_reasoning_tokens:usage.reasoningTokens,p_request_count:Math.max(1,attempts.length),p_actual_cost_microusd:actualCostUsd==null?null:Math.max(0,Math.ceil(actualCostUsd*1_000_000)),p_cost_source:actualCostUsd==null?(gateway?'VERCEL_REPORT_RECONCILIATION_REQUIRED':'DIRECT_PROVIDER_COST_UNPRICED'):'VERIFIED_GATEWAY_RESPONSE',p_metadata:{final_provider:provider,final_model:clean(result?.model,160)||null,auth_mode:clean(result?.auth_mode,80)||null,core_cost_mode:clean(result?.cost_mode,80)||null,gateway_user_attribution:gateway,attempts:attempts.slice(0,8),skipped_attempts:(skippedAttempts||[]).slice(0,8),billing_note:gateway?'Paid fallback is attributed to business_id in Vercel AI Gateway; exact report cost remains authoritative when response cost is absent.':'Direct-provider calls are metered by tokens; monetary cost is not guessed when the provider response has no billing amount.'}};const response=await fetch(`${SUPABASE_URL}/rest/v1/rpc/dabbir_record_ai_usage_v1`,{method:'POST',cache:'no-store',redirect:'manual',headers:supabaseKeyHeaders(key,{accept:'application/json','content-type':'application/json',prefer:'return=representation'}),body:JSON.stringify(body),signal:AbortSignal.timeout(8000)});if(!response.ok)throw new Error(`AI_USAGE_METER_HTTP_${response.status}`);return response.json().catch(()=>({ok:true}))}
export async function generateDABBIRAiReply(args={}){const identity=contextIdentity(args.meteringContext||args.businessContext),attempts=[],skippedAttempts=[];let successfulPayload=null,successfulResponse=null;const upstreamFetch=args.fetchImpl||fetch;const meteredFetch=async(url,options={})=>{let nextOptions=options;const endpoint=String(url||''),provider=providerForEndpoint(endpoint),circuitKey=cooldownKey(provider,options?.headers,upstreamFetch);let requestedModel=null;if(options?.body){try{const parsed=JSON.parse(String(options.body));requestedModel=clean(parsed?.model,160)||null;if(endpoint===GATEWAY_ENDPOINT&&identity.businessId){parsed.providerOptions={...(parsed.providerOptions||{}),gateway:{...(parsed.providerOptions?.gateway||{}),user:identity.businessId,tags:['channel:whatsapp','feature:customer-ai-reply']}};nextOptions={...options,body:JSON.stringify(parsed)}}}catch{}}if(provider!=='vercel-ai-gateway'){const remaining=cooldownRemainingMs(circuitKey);if(remaining>0){skippedAttempts.push({provider,model:requestedModel,reason:'PROVIDER_COOLDOWN',retry_after_ms:remaining});throw Object.assign(new Error('PROVIDER_COOLDOWN'),{code:'PROVIDER_COOLDOWN'})}}const started=Date.now();let response;try{response=await upstreamFetch(url,nextOptions)}catch(error){if(['SEMANTIC_PROVIDER_BUDGET','SEMANTIC_PROVIDER_RESERVED','PROVIDER_COOLDOWN'].includes(error?.code)){if(error?.code!=='PROVIDER_COOLDOWN')skippedAttempts.push({provider,reason:error.code});throw error}const attempt={endpoint:provider,model:requestedModel,status:0,duration_ms:Date.now()-started,outcome:'NETWORK_ERROR'};if(provider!=='vercel-ai-gateway'){const observed=observeProviderNetworkFailure(circuitKey);attempt.cooldown_ms=observed.cooldownMs;attempt.network_strike=observed.strikeCount}attempts.push(attempt);throw error}const attempt={endpoint:provider,model:requestedModel,status:Number(response?.status)||0,duration_ms:Date.now()-started};if(provider!=='vercel-ai-gateway'){if(response?.status===429){const observed=observeProvider429(circuitKey,response);attempt.retry_after_ms=observed.retryAfterMs;attempt.cooldown_ms=observed.cooldownMs;attempt.rate_limit_strike=observed.strikeCount}else clearProviderFailures(circuitKey)}attempts.push(attempt);if(response?.ok){try{successfulPayload=await response.clone().json();successfulResponse=response}catch{}}return response};const startedAt=Date.now(),coreResult=await generateCoreReply({...args,fetchImpl:meteredFetch}),reportedUsage=successfulPayload?.usage,hasUsage=reportedUsage&&[reportedUsage.prompt_tokens??reportedUsage.input_tokens,reportedUsage.completion_tokens??reportedUsage.output_tokens].every(v=>typeof v==='number'&&Number.isFinite(v)&&v>=0),actualCostUsd=coreResult?.provider==='vercel-ai-gateway'?actualGatewayCost(successfulPayload||{},successfulResponse):null;const result={...coreResult,telemetry:{final_request_usage:hasUsage?usageFromPayload(successfulPayload):null,actual_cost_usd:actualCostUsd,latency_ms:Date.now()-startedAt,request_count:attempts.length,attempts:attempts.map(a=>({provider:a.endpoint,model:a.model||null,status:a.status,latency_ms:a.duration_ms,retry_after_ms:a.retry_after_ms??null,cooldown_ms:a.cooldown_ms??null,rate_limit_strike:a.rate_limit_strike??null,network_strike:a.network_strike??null})),skipped_attempts:skippedAttempts}};if(!result?.ok){console.warn('dabbir_whatsapp_ai_provider_chain_failed',{attempts:attempts.slice(0,8).map(a=>({provider:a.endpoint,status:a.status,duration_ms:a.duration_ms,outcome:a.outcome||'HTTP_RESPONSE'})),skipped_attempts:skippedAttempts.slice(0,8).map(a=>({provider:a.provider,reason:a.reason,retry_after_ms:a.retry_after_ms||null})),configured_attempts:attempts.length});return result}if(!identity.businessId)return result;const usage=usageFromPayload(successfulPayload||{}),operationKey=`wa-ai-usage:${hash([identity.businessId,identity.conversationId,identity.messageTimestamp,clean(args.message,2000)].join('|')).slice(0,48)}`;await recordUsage({businessId:identity.businessId,operationKey,result,attempts,skippedAttempts,usage,actualCostUsd}).catch(error=>console.warn('dabbir_whatsapp_ai_meter_failed',{error:clean(error?.message||error,120),provider:clean(result?.provider,80)}));return result}
