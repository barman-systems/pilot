# S07 — تنفيذ التجهيز والتحقق الفعلي، 2026-09-13

## النتيجة

أُزيل مانع توفر PostgreSQL باستخدام عامل GitHub Actions مؤقت على فرع مستقل، بلا أسرار Production أو اتصال بقواعدها. لا دمج ولا تعديل v0.2 أو v1، ولم تنفذ تجربة S07.

الجولة المعتمدة لهذا التقرير: **3 اختبارات / 3 ناجحة / صفر تخطي** على الرأس `e9000f5bc3c8d7bb154b93ad83f208f6573e40ef`، [تشغيل 34758714922](https://github.com/barman-systems/pilot/actions/runs/34758714922). لا تجمع هذه الأعداد مع الجولات السابقة. اختبارات S07 المحلية الحالية منفصلة: 8/8.

PostgreSQL الفعلي: `16.15 (Ubuntu 16.15-1.pgdg24.04+2)`. بصمات pg_dump وpg_restore وpsql والمنفّذ والأرشيف في evidence.json. صورة العامل وNode وتفاصيل تثبيت الاعتماديات مثبتة في سجل التشغيل؛ إعادة التشغيل لاحقًا قد تجلب تحديثات Ubuntu، ولذلك يلزم إعادة التحقق من مطابقة البصمات قبل اعتماد بيئة تجربة مختومة.

## الدليل المنفذ

| البند | ما تحقق فعليًا | حد الدليل |
|---|---|---|
| A1 / A2 | مغلقان وفق المراجعة السابقة | لم نعد تشغيل جولتهما أو نضف أعدادها لهذه الجولة |
| A3 | استيراد B المجهز كاملًا؛ مسارا rpc وpreloaded يرفضان غياب الدليل وفشل كتابة PostgreSQL والتوقيع المزور؛ لا يصل المفسر إلا بعد حفظ الدليل. اختبر أيضًا RPC الحقيقي dabbir_semantic_load_v2 من SQL B عبر البوابة نفسها | المفسر نقطة توقف اختبارية؛ لا تشغيل سيناريو S07 أو مزود ذكاء؛ المفتاح محلي للاختبار |
| A4 | إنشاء 19 خطوة SQL من اختبار البناء B الأصلي على PostgreSQL فعلي، وإصدار قائمة الأعمدة، واستدعاء RPC الحقيقي. جلب A وB برأسيهما الكاملين وتثبيت اعتمادياتهما من lockfiles؛ استيراد A نجح | مخطط تجهيز اصطناعي موجود في مصدر B، وليس شهادة مطابقة DDL الإنتاج التاريخي. حزمة تشغيل S07 النهائية وتفويض الشاهد لم يعتمدا |
| A5 | المنفّذ نفسه يصدر ويستعيد قاعدة فعلية؛ جميع الصفوف والأعمدة في 35 جدولًا ضمن public/dabbir_private/auth متساوية قبل/بعد. رُفض الهدف المشغول والتفويض الغائب وكُشف فقد time في الاختبار المنفصل | مقارنة تجهيز اصطناعية، لا شهادة مساواة مستقلة لتجربة S07 |

بصمة الحالة لكامل نطاق الجداول في هذه الجولة قبل الاستعادة وبعدها: `c780be434ea55cbb06ae77d946d484d973d2aaf8208694c28bc76cf7ce4bd2d4`.

المصدر A: `ffde7cd866661b0091269a6b567f9a6460cf89e8`.
المصدر B الأصلي: `7645f9df82fc8c365f42451b949f387ec24db3d5`.
نسخة الرصد منفصلة في instrumented-B؛ بصمتها وبصمة الأصل محفوظتان في boundary.json. لا تغيير على الأصل.

## ملفات الدليل

- evidence/s07-native-20260913/evidence.json — قاعدة البيانات والأدوات واختبار المنفّذ.
- evidence/s07-native-20260913/boundary.json — حالات البوابة الثماني وبصمتا B.
- evidence/s07-native-20260913/sql-bootstrap.json — خطوات SQL وبصماتها والأعمدة وRPC ومقارنة الجداول الخمسة والثلاثين.
- .github/workflows/s07-native-preparation.yml — طريقة إعادة الاختبارات؛ لا مسار Production ولا مفاتيحها.

تعذّر تنزيل ZIP مباشرة إلى مساحة العمل؛ نُقلت JSON من سجل التشغيل الذي نشر محتوى الأدلة نفسه. السجل والأثر الأصلي متاحان في تشغيل GitHub. لم نستبدلهما بنتائج مكتوبة يدويًا.

المحاولة الأولى `34758177792` فشلت عند بدء PostgreSQL ولم تنفذ الاختبار. نجح البدء بعد تخصيص مجلد socket يملكه العامل؛ احتُفظ بالفشل ولم يُمحَ من السجل.

## المتبقي دون ادعاء إغلاق

1. ربط حزمة S07 النهائية بنطاقها وبيئتها المطابقين للتوقع المختوم، بما يشمل إعدادات المفسر والشاهد. نجاح مخطط اختبار B لا يثبت وحده مطابقة البيئة التاريخية، ولا يشغّل S07 تلقائيًا.
2. B1–B4: جهة تشغيل مستقلة، مرجع خارجي حقيقي، إعادة التفويض، والجفة المستقلة. GitHub Actions في مستودع الفريق لا يحقق الاستقلال الإداري.
3. كشف rollback شامل لا يزال مشروطًا بالمرجع الخارجي؛ لا يُغلق باختبار PostgreSQL.

تصحيح تجهيز إضافي: استُبدلت أسماء service-test-01 وما يماثلها بمعرّفات UUID اصطناعية محددة في محوّل الحالة والتوقع غير المختوم، لملاءمة أنواع SQL. اجتاز المحوّل مساواة مخرجه للملف ورفض الاسم القديم؛ لم ينفذ المحرك.

الحالة: `BLOCKED_PREREGISTRATION_ANCHOR`؛ الكتل 6/2؛ بروفة تجهيز فعلية ناجحة، والقبول المستقل غير حاصل. لا نسبة للاختبارات إلى واقعة S07 ولا تغطية freeze مستقلة.
