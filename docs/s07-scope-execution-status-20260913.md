# S07 — نتيجة تجهيز النطاق

نجحت الجولة 34761586351 على c8de4a212d8ff771db3097ee608fd9a29edd46ee: 3/3 اختبارات أصلية على PostgreSQL فعلي، بلا تخطٍ. الأعداد تخص هذه الجولة وحدها.

- جامع نطاق معلن: 33 جدولًا و254 عمودًا، داخل REPEATABLE READ READ ONLY. مطابقة مخطط الأسماء والأنواع وترتيب الأعمدة، ورفض إضافة عمود غير معلن.
- بصمتا الحالة قبل التصدير وبعد الاستعادة قبل التحميل متساويتان. فحص الاستعادة الشامل السابق لـ35 جدولًا باقٍ، والنطاق الجديد يفصل الحسابات ودليل الاختبار.
- تغييرات الجامع والنطاق وخطوات SQL تشغّل الآن اختبار التجهيز الآلي.
- حُفظت النتائج الخام في docs/evidence/s07-native-34761586351.json. التوقعات بقيت مسودة غير مختومة، مع إحالة إلى النطاق المثبت.

## حدود الإغلاق

توفر PostgreSQL واختبار الاستعادة الاصطناعية مثبتان؛ لا يصح إسناد التعطيل الحالي إلى عدم توفر القاعدة. اختبار B المجهز يصل إلى حد التسليم ويتوقف قبل المفسر؛ لا يثبت تجربة S07 كاملة. جامع النطاق مثبت وظيفيًا، وربطه بشهادة الشاهد المستقل والتفويض النهائي لم يُثبت. إعداد المزود والنموذج وسياسة fallback للجولة الفعلية يحتاج تثبيتًا قبل الختم؛ استيراد اعتماديات المصدر لا يثبت تلك الإعدادات.

B1 لم يُسمّ فيه مشغل مستقل؛ B2–B4 ليست منجزة. لا شهادة مساواة مستقلة، ولا تشغيل للأذرع، ولا دمج أو تعديل Production أو v0.2 أو v1. الحالة BLOCKED_PREREGISTRATION_ANCHOR، والكتل 6/2.
