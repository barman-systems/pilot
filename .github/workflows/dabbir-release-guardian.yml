name: DABBIR Release Guardian

on:
  workflow_dispatch:

# Frozen by owner directive on 2026-09-15.
# No automatic PR closing, workflow reruns, rollback branches, or source-control
# mutations during intelligence-only product development.
permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'DABBIR Release Guardian is frozen; product intelligence CI is authoritative.'
