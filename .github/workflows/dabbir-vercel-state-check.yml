name: DABBIR Vercel State Check

on:
  workflow_dispatch:

# Frozen by owner directive on 2026-09-15. Vercel governance/state diagnostics
# no longer run automatically during intelligence-only product development.
permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Vercel state-check automation is frozen.'
