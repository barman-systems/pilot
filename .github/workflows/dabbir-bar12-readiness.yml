name: DABBIR BAR-12 Readiness

on:
  workflow_dispatch:

# Frozen by owner directive on 2026-09-15. Readiness/governance work is outside
# the current intelligence-only product focus.
permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'BAR-12 readiness automation is frozen.'
