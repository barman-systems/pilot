name: DABBIR Main Lineage Guard

on:
  workflow_dispatch:

# Frozen from automatic execution by owner directive on 2026-09-15.
permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Main-lineage governance is frozen during intelligence-only development.'
