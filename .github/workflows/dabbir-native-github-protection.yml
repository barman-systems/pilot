name: DABBIR Native GitHub Protection Bootstrap

on:
  workflow_dispatch:

# Frozen from automatic execution by owner directive on 2026-09-15.
# Existing main protection is left in place; this workflow no longer mutates it automatically.
permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Native GitHub protection bootstrap is frozen; existing main protection is unchanged.'
