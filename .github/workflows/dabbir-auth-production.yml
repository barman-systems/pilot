name: DABBIR Auth Production Guard

on:
  workflow_dispatch:

# Frozen from automatic execution by owner directive on 2026-09-15.
# Existing runtime authentication protections remain in application code; this
# repository gate is retained only as a manual diagnostic entrypoint.
permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Auth Production Guard automation is frozen during intelligence-only development.'
