export const DABBIR_RECOVERY_CENTER_CONTRACT = Object.freeze({
  version: 'v3',
  default_table_policy: 'reconcile_only',
  account_must_be_suspended: true,
  automatic_apply_requires_zero_reconciliation_events: true,
  customer_facing_side_effects_allowed: false,
});
